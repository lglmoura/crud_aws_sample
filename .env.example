# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# docker container run -d -p 5432:5432 --name fullstack_dev  -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=1q2w3e4r -e POSTGRES_DB=fullstack_db  postgres
# docker container run -d -p 8080:80 -e PGADMIN_DEFAULT_EMAIL=lglmoura@gmail.com -e PGADMIN_DEFAULT_PASSWORD=1q2w3e4r dpage/pgadmin4

DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=aws_db

DB_PORT=5432
#DB_HOST=localhost
DB_HOST=aws_psql

#DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@POSTGRES_HOST:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

PGADMIN_USER=
PGADMIN_PASSWORD=

DOCKER_TARGET=development

APP_PORT=3001